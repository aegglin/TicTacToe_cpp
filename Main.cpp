#include <iostream>
#include "TicTacToeSimulator.h"

int main()
{
    TicTacToeSimulator simulator;
    std::cout << simulator.getSimMove().first << simulator.getSimMove().second << std::endl;
    std::cout << simulator.getSimMove().first << simulator.getSimMove().second << std::endl;
    std::cout << simulator.getSimMove().first << simulator.getSimMove().second << std::endl;
    std::cout << simulator.getSimMove().first << simulator.getSimMove().second << std::endl;
    std::cout << simulator.getSimMove().first << simulator.getSimMove().second << std::endl;

}